name: core
description: 共有パッケージ(配管のみ) — APIクライアント・Session・配色定数
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
